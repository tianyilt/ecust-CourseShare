# 保留位即近似后的最低位，
# 近似位即保留位的后一位（作判断用），
# 粘滞位是近似位后的所有位进行或运算后视作一位。
# 单精度格式(32 位)：E = 8 位，M = 23 位。
# 符号位 S，指数部分 E 和尾数部分 M。
# 算出无限精度的值
# 按二进制写
# 砍掉 23 位后面的，注意保护位，舍入位，粘贴位。
# 向上近似只有两种情况：
'''
近似位为 1，粘滞位为 1。
以 0.RXX 表示近似位和粘滞位的话，0.RXX>0.5，即进位，无需多言。
'''

'''
example
10101010111  |  1 | 011
          G     R    S
          1     1    1
R为 1，S为 1。
G为 1，R为 1，S为 0
'''
